<div id="home">
﻿<div class = "after_check">
	<h1>サークル一覧</h1>
	<%unless @msg.blank?%>
	<% @msg.each do |obj| %>
	<figure class ="picture_box">
		<%= image_tag obj.image.to_s,:class=>'picture_size' %>
		<figcaption><%= obj.username %></figcaption>
	</figure>
	<%end%>
	<%else%>
		<p>まだ、サークルが登録されていないみたいです。もうしばらくお待ちください。</p>
	<% end %>
<!--
<table>
	<%unless @msg.blank?%>


<tr>
	<th>ID</th><th>Name</th><th>Genre</th><th>Image</th>
</tr>
	<% @msg.each do |obj| %>
	<tr>
	<td><%= obj.id %></td>
	<td><%= obj.username %></td>
	<td><%= obj.genre.name %></td>
	<td><%= image_tag obj.image.to_s%></td>
	</tr>

	<%end%>
	<%else%>
	<p>まだ、サークルが登録されていないみたいです。もうしばらくお待ちください。</p>
	<% end %>
</table>
-->
</div>
</div>
